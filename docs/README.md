# Docs

OpsPilot AI documentation lives in this folder.

Phase 1.6 dataset note:

- Routing/category label: `true_category`
- Priority label: `true_priority`
- Ticket type: `ticket_type`
- Reference answer: `reference_answer`
- Tags are stored as pipe-separated text in `tags` for the MVP.

The normalized English dataset is expected at `data/processed/tickets_en_normalized.csv`.
